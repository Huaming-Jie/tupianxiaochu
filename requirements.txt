# ============================================================================
# 智能图像编辑器 —— 依赖清单
# ----------------------------------------------------------------------------
# 依赖被分成三档。第 1 档装完程序就能跑（会自动降级到 OpenCV 修复）；
# 第 2 档决定修复质量；第 3 档是可选的重型增强。
#
# 国内安装建议加清华镜像：
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# ============================================================================

# ---------------------------------------------------------------------------
# 【第 1 档 · 必装】没有这些程序无法启动
# ---------------------------------------------------------------------------
numpy>=1.24,<3.0
opencv-python>=4.8.0            # 若在无显示环境，可换成 opencv-python-headless
Pillow>=10.0.0
PyQt5>=5.15.9                   # 图形界面
PyMuPDF>=1.23.0                 # PDF 读写（fitz）
requests>=2.31.0                # 模型权重下载
tqdm>=4.66.0

# ---------------------------------------------------------------------------
# 【第 2 档 · 强烈推荐】LaMa 深度修复，决定"擦除"的质量上限
#   CPU 版即可，约 200 MB 权重，512×512 推理 1~3 秒
#   CPU-only 安装（体积小很多）：
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
#   CUDA 12.1 版：
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
# ---------------------------------------------------------------------------
torch>=2.0.0
# onnxruntime>=1.16.0           # 备选后端：没装 torch 时启用（二选一即可）

# ---------------------------------------------------------------------------
# 【第 3 档 · 可选增强】按需安装
# ---------------------------------------------------------------------------

# --- OCR：文字消除 / 文字修改 / 水印文本线索 需要它 -------------------------
# 方案 A（中文精度最好，推荐）：
#   pip install paddlepaddle          # CPU 版
#   pip install paddleocr
# 方案 B（安装更轻）：
#   pip install easyocr
#
# paddlepaddle>=2.5.0
# paddleocr>=2.7.0
# easyocr>=1.7.0

# --- Stable Diffusion 生成式修复：大面积缺失、需要"造"内容时使用 -----------
#   需要 NVIDIA GPU，显存 ≥ 6 GB，权重约 2.5 GB
# diffusers>=0.25.0
# transformers>=4.36.0
# accelerate>=0.25.0
# safetensors>=0.4.0

# --- 提速（可选） ----------------------------------------------------------
# xformers                       # 降低 SD 显存占用
# scikit-image>=0.21.0           # 若需扩展更多图像度量
